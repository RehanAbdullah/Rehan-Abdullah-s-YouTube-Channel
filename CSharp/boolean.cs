using System;

class Boolean
{
	public static void Main()
	{
		bool content   = true;
		bool noContent = false;

		Console.WriteLine("It is {0} that C# is an OO language.", content);
		Console.WriteLine ("The Statement above is NOT {0}", noContent);

	}
}
